#include "../header/base.h"
#include "../header/levelSelector.h"

int main() {

    // Nous aurons donc stocker dans nomLevel le Level qui sera jouer par l'utilisateur;
    char* nomLevel = choixLevel();
    
    return 0;
}